<%= form_with(model: measurement, local: true) do |form| %>
  <% if measurement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measurement.errors.count, "error") %> prohibited this measurement from being saved:</h2>

      <ul>
      <% measurement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bust %>
    <%= form.text_field :bust, id: :measurement_bust %>
  </div>

  <div class="field">
    <%= form.label :waist %>
    <%= form.text_field :waist, id: :measurement_waist %>
  </div>

  <div class="field">
    <%= form.label :hip %>
    <%= form.text_field :hip, id: :measurement_hip %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
